# DIMA 웹앱 프로토타입 구현 계획

## 개요

**DIMA (Design Integrity & Manufacturability AI)** 웹 프로토타입.
DXF(2D) 및 STL(3D) 파일을 브라우저에서 직접 업로드하여 설계 검증 결과를 시각화하는 단일 페이지 웹앱.

> [!IMPORTANT]
> 프로토타입 범위: AI 모델 대신 **Rule Engine + 시뮬레이션 데이터**로 구현.
> 실제 AI 연동은 Phase 2에서 진행.

---

## 기술 스택

| 항목 | 선택 | 이유 |
|------|------|------|
| 기본 | HTML5 + CSS3 + Vanilla JS | 설치 불필요, 즉시 실행 가능 |
| 3D 렌더링 | Three.js (CDN) | STL 뷰어 + 색상 오버레이 |
| DXF 파싱 | dxf-parser (CDN) | 브라우저에서 DXF Entity 추출 |
| 차트 | Chart.js (CDN) | 분석 결과 게이지/레이더 차트 |
| 폰트 | Google Fonts - Outfit | 프리미엄 타이포그래피 |

---

## 화면 구성

```
┌─────────────────────────────────────────────────────┐
│  DIMA  [로고]          [2D 검증] [3D 검증]  [리포트] │  ← 헤더 네비
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌────────────────┐  ┌───────────────────────────┐  │
│  │  파일 업로드   │  │   뷰어 (2D Canvas / 3D)   │  │
│  │  드래그&드롭   │  │                           │  │
│  │                │  │                           │  │
│  ├────────────────┤  └───────────────────────────┘  │
│  │  분석 결과     │  ┌───────────────────────────┐  │
│  │  ✅ 통과 항목  │  │  점수 게이지 + 이슈 리스트 │  │
│  │  ❌ 오류 항목  │  │                           │  │
│  └────────────────┘  └───────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

---

## 모듈별 기능 (프로토타입 범위)

### 📐 2D 모듈 (AutoCAD DXF)
- **파일 입력**: `.dxf` 드래그&드롭 업로드
- **파싱**: dxf-parser로 Entity(Line, Circle, Arc, Text) 추출
- **Canvas 렌더링**: 도면을 브라우저에 직접 그리기
- **룰 검증** (Rule Engine):
  - 레이어 표준 준수 여부 (0, CENTER, HIDDEN, DIMENSION 등)
  - 치수 텍스트 누락 감지
  - 중심선(Center Line) 존재 여부
  - 숨은선 레이어 분리 여부
- **결과**: 이슈 목록 + 심각도(Error/Warning/Info) 표시

### 🧊 3D 모듈 (STL 파일)
- **파일 입력**: `.stl` 드래그&드롭 업로드
- **Three.js 렌더링**: 3D 모델 뷰어 (회전/줌/팬)
- **룰 검증** (시뮬레이션):
  - 구배각(Draft Angle) 분석 → 색상 오버레이 (녹색=OK / 빨강=위험)
  - 살두께(Wall Thickness) 추정 → 수축 위험도 %
  - 언더컷 의심 구간 감지
  - 소재 선택 (ABS / PC / PP) → 수축률 가이드
- **결과**: 종합 품질 점수 (0~100) + 상세 이슈 리스트

---

## 디자인 컨셉

- **테마**: 다크 모드 (우주/공학 느낌)
- **컬러**: 딥 네이비 배경 + 사이언 액센트 (`#00D4FF`)
- **스타일**: 글래스모피즘 카드, 네온 글로우 효과
- **애니메이션**: 파일 드롭 시 파티클 효과, 분석 중 로딩 스피너, 결과 카운트업

---

## 파일 구조

```
AUTO_CAD_TOOL/
├── index.html          ← 메인 진입점
├── css/
│   └── style.css       ← 전체 스타일 (다크 테마 + 애니메이션)
└── js/
    ├── main.js         ← 앱 초기화 + 탭 라우팅
    ├── dxf-analyzer.js ← 2D DXF 파싱 + 룰 엔진
    ├── stl-analyzer.js ← 3D STL 분석 + 시뮬레이션
    └── renderer.js     ← Three.js 뷰어
```

---

## 개발 순서

1. `index.html` + `style.css` → 전체 레이아웃 + 다크 테마
2. 탭 전환 + 파일 업로드 UI
3. 2D: DXF 파서 연동 + Canvas 렌더링 + 룰 검증
4. 3D: Three.js STL 뷰어 + 색상 오버레이 분석
5. 결과 대시보드 (Chart.js 게이지)
6. 전체 폴리시 + 애니메이션

---

## 검증 계획

- 실제 DXF 파일로 파싱 테스트
- STL 샘플 파일 (단순 박스, 언더컷 있는 형상)로 3D 분석 테스트
- 브라우저 오픈 후 UI/UX 검토

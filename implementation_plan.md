# DIMA 웹앱 프로토타입 구현 계획 (2D 도면 즉시 화면 렌더링 및 뷰포트 리사이즈 개선)

## 개요
2D 도면(DXF/DWG) 파일을 업로드했을 때 화면에 렌더링되지 않거나, 브라우저 창 크기 조절/사이드바 크기 조절 시 도면이 찌그러지거나 잘리는 현상을 해결합니다. 
사이드바를 마우스로 직접 조절할 수 있는 리사이저 바를 구현하고, 윈도우 리사이즈 및 탭 전환 시 캔버스 해상도가 자동으로 맞춰지며 도면이 동적으로 리렌더링되는 반응형 CAD 뷰포트를 완성합니다. 또한 누락되었던 하단 CAD 메시지 콘솔을 복원하여 실시간 파싱 및 오류 상황을 모니터링할 수 있도록 합니다.

> [!IMPORTANT]
> **주요 수정 및 개선 사항**
> 1. **사이드바 드래그 리사이저 구현**: 마크업만 존재하던 `.cad-resizer` 바의 스타일을 적용하고, 마우스 드래그를 통해 좌/우 사이드바의 너비를 자유롭게 조절할 수 있도록 스크립트를 연동합니다.
> 2. **리사이즈 및 탭 전환 시 자동 갱신**: 창 크기 변경, 사이드바 드래그, 탭 전환 시 캔버스의 실제 내부 해상도(`width`, `height` 어트리뷰트)를 물리적 뷰포트 크기와 일대일 매칭시키고, 도면을 자동으로 `fitView`하여 왜곡 없이 깨끗하게 렌더링되도록 합니다.
> 3. **CAD 메시지 콘솔 복원**: `js/main.js`에 정의되어 있지만 `index.html`에 누락되었던 하단 메시지 로그 콘솔 영역을 복원하여 사용자가 로그를 확인할 수 있도록 합니다.

---

## 핵심 변경 내용 및 알고리즘 설계

### 1. 사이드바 리사이저 (Drag to Resize)
- 좌측 및 우측 사이드바와 뷰포트 사이의 `.cad-resizer` 요소에 마우스 드래그 이벤트를 바인딩합니다.
- 드래그 중 실시간으로 사이드바의 `width`를 조절하고, 변경된 너비에 맞게 중앙 뷰포트 캔버스의 너비와 높이를 즉시 재계산하여 다시 그립니다(`DXFAnalyzer.draw()`, `STLAnalyzer.resizeViewer()`).

### 2. 반응형 캔버스 해상도 조절 (Resize Handling)
- 윈도우의 `resize` 이벤트가 발생하거나 탭이 전환될 때, 현재 활성화된 탭의 캔버스 크기를 구동하고 도면을 피팅하여 다시 그리는 `triggerViewportResize()` 함수를 추가합니다.
- 3D 뷰어 또한 브라우저 창이나 사이드바 크기가 변할 때 쓰리디스(Three.js)의 렌더러와 카메라 종횡비(`aspect`)가 업데이트되도록 연동합니다.

---

## Proposed Changes

### [Web UI & Styles Component]

#### [MODIFY] [index.html](file:///c:/Users/cwd10/AUTO-CAD-TOOL/index.html)
- 앱 하단 영역에 CAD 디버그 메시지 콘솔(`.cad-message-console`) 마크업 추가.
- 리사이저 바 구조 점검 및 필요시 클래스명 매칭.

#### [MODIFY] [css/style.css](file:///c:/Users/cwd10/AUTO-CAD-TOOL/css/style.css)
- `.cad-resizer` (사이드바 경계 조절바) 스타일 지정 (마우스 호버 시 cyan 색상 글로우 및 `col-resize` 커서 적용).
- 복원된 `.cad-message-console` 및 하단 콘솔 로그박스의 어둡고 미려한 프리미엄 테마 스타일링 추가.

### [2D/3D Viewport Resizing & Event Component]

#### [MODIFY] [js/main.js](file:///c:/Users/cwd10/AUTO-CAD-TOOL/js/main.js)
- 사이드바 리사이저 드래그 동작 로직 구현 (`initSidebarResizer`).
- 윈도우 리사이즈 및 탭 전환 시 2D/3D 캔버스 크기를 뷰포트에 맞추고 새로 고침하는 반응형 리사이즈 핸들러 구현.
- `handle2DFile` 및 탭 전환 시 뷰포트 크기가 0이 아닌 유효한 순간에 맞추어 캔버스 해상도를 세팅하도록 보완.

---

## Verification Plan

### Automated & Manual Verification
1. **2D 도면 업로드 및 초기 출력 검증**
   - `.dxf` 또는 `.dwg` 파일을 드래그하여 로드했을 때 2D 뷰포트에 도면이 왜곡이나 흐려짐 없이 선명하게 나타나는지 확인합니다.
2. **사이드바 드래그 리사이즈 검증**
   - 좌/우측 사이드바 경계선을 클릭 드래그하여 너비를 줄이거나 늘려봅니다.
   - 드래그 동작에 맞춰 중앙 도면 캔버스의 너비가 실시간으로 부드럽게 변하며 도면이 깨지지 않고 캔버스 크기에 딱 맞춰 유지되는지 확인합니다.
3. **윈도우 리사이즈 및 탭 전환 검증**
   - 브라우저 창 크기를 키우거나 줄였을 때 2D/3D 화면이 자동으로 갱신되어 화면 비율을 유지하는지 확인합니다.
   - 3D 탭으로 이동했다가 다시 2D 탭으로 돌아왔을 때 도면이 사라지지 않고 즉시 동일한 화면 비율로 복원되어 보이는지 확인합니다.
4. **CAD 메시지 콘솔 로그 출력 검증**
   - 화면 하단에 디버그 메시지 콘솔창이 표시되고, 파일을 올리거나 조작할 때마다 로그 메시지가 실시간으로 추가되는지 확인합니다.
